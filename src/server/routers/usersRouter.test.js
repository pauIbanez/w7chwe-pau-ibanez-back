describe("Given users/register endpoint", () => {
  describe("When it's passed a valid user", () => {
    test("Then it should respond with 201 and the created user Id", () => {
      // Arrange
      // Act
      // Assert
    });
  });
});
